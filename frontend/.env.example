# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_V1_PREFIX=/api/v1

# Clerk Authentication (Get these from https://dashboard.clerk.com)
# Frontend (publishable key - safe to expose in browser)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Backend (secret key - keep secure, only for backend API calls if needed)
# Note: The backend uses CLERK_SECRET_KEY from its own .env file
# This is only needed if you're making Clerk API calls from Next.js server components
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Clerk URLs (optional - auto-detected if using standard routes)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/videos
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/videos

# Development: Disable SSL verification for corporate environments
# IMPORTANT: Only use in development! Remove in production!
# This fixes "unable to get local issuer certificate" errors
NODE_TLS_REJECT_UNAUTHORIZED=0
